<Window x:Class="trabalhoPOO.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:trabalhoPOO.Views"
        mc:Ignorable="d"
        Title="LoginWindow" Height="450" Width="800">
    <Grid>
        
        <StackPanel Background="Gray" Width="200px" Margin="300,157,300,117" >
            <Label Content="Username"></Label>
            <TextBox Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <Label Content="Password"></Label>

            <PasswordBox x:Name="PasswordBoxField"/>

            <Button Content="Login" 
                    Command="{Binding LoginCommand}"
                    Margin="0,15,0,0">

                <Button.CommandParameter>
                    <Binding ElementName="PasswordBoxField"/>
                </Button.CommandParameter>
            </Button>
          <!--<Button Content="Login" Command="{Binding LoginCommand}" CommandParameter="{Binding Username}" />-->

        </StackPanel>
    </Grid>
</Window>
